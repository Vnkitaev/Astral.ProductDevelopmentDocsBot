import { isFunction } from 'lodash';

export class ApplicationCommands {
  public subscribe(bot) {
    // bot.command('start', base.start);
    // bot.command('help', base.help);
    //
    // bot.on('text', base.onText);
  }
}
